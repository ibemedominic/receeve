{
  "name": "mailgun",
  "version": "1.0.0",
  "description": "Receeve implementationf for Mailgun Webhools",
  "main": "app.js",
  "author": "Dominic Ibeme",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.49.0",
    "@aws-sdk/client-s3": "^3.49.0",
    "@aws-sdk/client-sns": "^3.49.0",
    "axios": "^0.21.1"
  },
  "scripts": {
    "build": "tsc && cp -R node_modules ../dist",
    "watch": "tsc -w",
    "test": "mocha tests/unit/"
  },
  "files": [
    "dist/"
  ],
  "devDependencies": {
    "@types/aws-lambda": "^8.10.92",
    "@types/node": "^17.0.15",
    "chai": "^4.2.0",
    "mocha": "^9.1.4",
    "typescript": "^4.5.5"
  }
}
